# VegaCore production — proxy everything to the Node app (API + React SPA + uploads)
# The Node server (port 3001) serves dist/, /api, and /uploads in one process.

server {
    listen 80;
    listen [::]:80;
    server_name vegacore.co www.vegacore.co;

    client_max_body_size 12M;

    location / {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# After certbot SSL, add a second server block for 443 with the same location / proxy.
